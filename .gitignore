node_modules
.env
.env.local
.env.*.local
dist
.next
out
*.tsbuildinfo
# Personal Claude state (settings.local.json etc.), but keep shared
# project skills and agents checked in so future sessions and teammates pick them up.
.claude/*
!.claude/skills
!.claude/agents

# Local-only working docs for in-flight work (spec drafts, running
# implementation notes). Stay out of git, surface in PR descriptions instead.
/SPEC-*.md
/IMPLEMENTATION-NOTES.md
# Repo-root /docs only (SPEC dump); does NOT match apps/web/content/docs.
/docs
coverage
.DS_Store
*.log
.turbo
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
